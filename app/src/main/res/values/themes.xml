<resources>
    <style name="Theme.Expense" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- MainActivity hosts the whole Compose UI; all visuals come from Compose.
         This AppCompat theme exists only so androidx.biometric can inflate its
         legacy dialog on API 26/27. -->
    <style name="Theme.Expense.Lock" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
